import React, { useState } from 'react';
import { css } from '@emotion/css';

const TierlistsEditor = () => {

    return(<>
        Tierlists editor...
    </>);
};

export default TierlistsEditor;

/**
 * CSS STYLES
 */
const viewer_header_style = css({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center'
});
